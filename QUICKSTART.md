# 🚀 Get_transform 快速开始指南

## 面向非技术用户的使用说明

### 什么是 Get_transform？

Get_transform 是一个工具，可以将 GET笔记 导出的文件进行整理：
- 将乱码文件名改成有意义的标题
- 删除重复的笔记文件
- 方便导入 Notion 等其他笔记软件

### 下载和运行

#### 1. 下载程序

访问 [Releases 页面](https://github.com/yorickhui/Get_transform/releases)，下载对应你系统的文件：

| 操作系统 | 下载文件 |
|---------|---------|
| Windows 10/11 | `get_transform.exe` |
| macOS | `get_transform` (macOS) |
| Linux | `get_transform` (Linux) |

#### 2. 运行程序

**Windows 用户:**
1. 双击下载的 `get_transform.exe` 文件
2. 如果 Windows 提示"无法识别的应用"，点击"更多信息" → "仍要运行"

**macOS 用户:**
1. 打开"终端"应用（在"应用程序" → "实用工具"中）
2. 输入以下命令并按回车：
   ```bash
   cd ~/Downloads
   chmod +x get_transform
   ./get_transform
   ```
3. 如果系统提示"无法验证开发者"，在"系统偏好设置" → "安全性与隐私"中点击"仍要打开"

**Linux 用户:**
1. 打开终端
2. 进入下载目录并运行：
   ```bash
   cd ~/Downloads
   chmod +x get_transform
   ./get_transform
   ```

#### 3. 首次运行配置

程序首次运行会引导你进行配置：

1. **选择导出路径**: 程序会询问你 GET笔记 导出文件的存放位置
   - 推荐使用默认路径（程序会自动创建 `history` 目录）
   - 或者指定你自己的路径

2. **自动创建目录**: 程序会自动创建必要的目录：
   - `history/`: 存放 GET笔记 导出的文件
   - `new/`: 存放处理后的文件（标题已改好）
   - `logs/`: 存放日志文件

### 使用流程

#### 步骤 1: 导出 GET笔记

1. 打开 GET笔记 应用
2. 导出你的笔记（选择 HTML 格式）
3. 解压导出的压缩包

导出的文件夹名称通常类似：
```
voicenotes_202501270930_getnotes_archive_xxxxx
```

#### 步骤 2: 放入 history 目录

将解压后的整个文件夹（包含 `notes` 文件夹和 `index.html`）复制到 `history` 目录下。

**目录结构示例:**
```
Get_transform/
├── get_transform.exe (或可执行文件)
├── history/
│   └── voicenotes_202501270930_getnotes_archive_xxxxx/
│       ├── notes/
│       │   ├── xxxxx.html
│       │   ├── xxxxx.html
│       │   └── ...
│       └── index.html
├── new/
└── logs/
```

#### 步骤 3: 运行程序

运行 `get_transform`，程序会显示菜单：

```
请选择操作:
1. 试运行 (查看将要删除的文件，不实际删除)
2. 正式运行 (实际删除重复文件)
3. 仅复制重命名 (不删除重复文件，仅将最新文件夹中的HTML文件复制到new目录并重命名)
4. 退出
```

**推荐流程:**

1. **首次使用**: 选择 `3` - 仅复制重命名
   - 这样只会复制并重命名文件，不会删除任何东西
   - 安全无风险

2. **再次导出笔记后**: 
   - 将新的导出文件夹也放到 `history` 目录
   - 选择 `1` - 试运行，查看会删除哪些重复文件
   - 确认无误后，选择 `2` - 正式运行

#### 步骤 4: 查看结果

处理完成后，在 `new` 目录中查看结果：

**之前（乱码文件名）:**
```
9323c2f00816d6a7cfffabb9b8ea1ad5.html
```

**之后（有意义的标题）:**
```
太变态！这个AI工具从写代码到发推啥都能干.html
如何使用 Notion 提高工作效率.html
```

现在你可以：
- 直接打开 HTML 文件查看笔记
- 将这些文件导入 Notion 等其他笔记应用
- 保存或分享这些笔记

### 配置和高级选项

即使是非技术用户，也只需记住以下要点：

| 内容 | 说明 |
|------|------|
| 配置文件 | 位于程序同级的 `.get_transform_config.json`，首次运行会自动生成 |
| 保存内容 | `history/new/logs` 路径 + 默认运行模式（试运行/正式运行/仅复制） |
| 如何修改 | 1）删除配置文件重新运行；2）用记事本直接修改 JSON；3）设置环境变量 `GET_HISTORY_PATH` 等临时覆盖 |
| 何时需要改 | 更换电脑、需要把 `history/` 放到移动硬盘或网盘、想让默认模式变成“仅复制” |

> 小技巧：如果分享给家人/同事，只需复制整个文件夹并重新运行程序，他们也会看到配置向导。

### 常见问题

#### Q: 程序运行后没有反应？

**A**: 
- Windows: 可能被杀毒软件拦截，请添加信任
- macOS: 需要在"安全性与隐私"中允许运行
- 检查是否有错误提示，查看 `logs` 目录中的日志文件

#### Q: 找不到 history 目录？

**A**: 
- history 目录应该和可执行文件在同一个目录下
- 首次运行程序会自动创建这个目录
- 如果没有自动创建，手动创建一个名为 `history` 的文件夹

#### Q: 文件名还是乱码？

**A**: 
- 确保导出的文件夹包含 `index.html` 文件
- `index.html` 文件包含了标题与文件名的映射关系
- 检查 logs 目录中的日志，看是否有错误信息

#### Q: 删除了不该删除的文件怎么办？

**A**: 
- 不要慌！原始文件仍然在 `history` 目录中
- 程序只删除 `history` 目录中的重复文件，不会删除 `new` 目录中的文件
- 如果在正式运行时选择了备份，备份文件会保存在原位置
- 下次可以先用"试运行"模式查看会删除什么

#### Q: 我需要保留 history 目录中的旧文件吗？

**A**: 
- **首次使用**: 可以删除（只有一个导出文件夹时）
- **长期使用**: 建议保留最近一次的导出文件夹
- 保留旧文件的好处：程序可以识别哪些是新笔记，哪些是重复的
- 如果空间不够，至少保留上一次的导出文件夹

#### Q: 可以在多台电脑上使用吗？

**A**: 
- 可以！可执行文件是独立的，可以复制到任何电脑
- 每台电脑首次运行都需要配置路径
- 配置会保存，下次不用重新设置

#### Q: 支持中文文件名吗？

**A**: 
- 完全支持中文、日文、韩文等各种语言
- 程序会自动处理特殊字符，确保文件名在所有系统上都能正常使用

### 获取帮助

如果遇到问题：

1. **查看日志**: `logs` 目录中的日志文件包含详细信息
2. **阅读文档**: 查看 [README.md](README.md) 获取更多信息
3. **报告问题**: 在 [GitHub Issues](https://github.com/yorickhui/Get_transform/issues) 提交问题
4. **联系作者**: yorickhui@gmail.com

### 提示和技巧

1. **定期导出**: 每周或每月导出一次 GET笔记，保持同步
2. **先试运行**: 使用"试运行"模式可以预览操作，更安全
3. **备份重要笔记**: 虽然程序很安全，但重要笔记最好额外备份
4. **清理旧文件**: 处理完成后，可以删除 history 中的旧版本（保留最新一个）
5. **批量导入**: 将 new 目录中的所有文件一次性导入 Notion 等应用

---

**祝你使用愉快！** 🎉

如果觉得这个工具有用，欢迎：
- ⭐ 在 GitHub 上给项目加星
- 📢 分享给有需要的朋友
- 💬 提供反馈和建议
